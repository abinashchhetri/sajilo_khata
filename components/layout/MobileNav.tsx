// Mobile navigation is now handled by the side drawer inside Navbar.
// This component is kept as a no-op export to avoid breaking any stale imports.
const MobileNav = () => null;
export default MobileNav;
